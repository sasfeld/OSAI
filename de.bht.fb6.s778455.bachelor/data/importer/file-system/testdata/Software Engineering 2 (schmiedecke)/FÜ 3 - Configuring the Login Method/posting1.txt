CREATION_DATETIME: 1384087241
CONTENT: 
hallo,

bei der Fingerübung 3 hänge ich an einer stelle fest. Ich arbeite mit dem GlassFish Server und Netbeans IDE 7.3.1

bei Schritt "Configuring the Login Method" soll man in der web.xml unter Security die Login Configuration und anschließend die Security Roles anpassen.

In meinem Projekt befindet sich nur eine GlassFish-web.xml. Diese hat jedoch keinerlei Login Configuration und oder Security Role wie im Fenster, sondern lediglich ein Security Role Mapping, das allerdings nicht die richtigen benötigten Einstellungen enthält. Ich kann so auch keinen realm einstellen, habe keine Constraints etc etc

Eine eigene web.xml kann ich nicht erstellen, das mir dann angezeigt wird, dass schon eine existiert. Ich bin unsicher, wie ich mit der glassfish-web.xml verfahren soll und google war in diesem Fall nicht mein Freund :o)

hat jemand das gleiche Problem oder eine Idee?

Danke schonmal für die Hilfe